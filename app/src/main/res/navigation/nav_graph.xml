<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.screening.productivityapp.ui.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_detailTaskActivity"
            app:destination="@id/detailTaskActivity" />
    </fragment>
    <activity
        android:id="@+id/detailTaskActivity"
        android:name="com.screening.productivityapp.detail.DetailTaskActivity"
        android:label="activity_detail_task"
        tools:layout="@layout/activity_detail_task" />

</navigation>